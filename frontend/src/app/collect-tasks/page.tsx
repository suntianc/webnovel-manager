import { PlaceholderPage } from "@/components/layout/PlaceholderPage";

export default function CollectTasksPage() {
  return (
    <PlaceholderPage
      description="下载功能已移出当前范围，后续这里可用于展示本地上传小说的解析、分章与素材提取任务。"
      title="采集任务管理"
    />
  );
}
